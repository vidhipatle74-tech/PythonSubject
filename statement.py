#Flow control statements:
'''
#-->*Controlling the flow of program execution is called flow control statements.

#-->*flow control statement is classified into 2 types,

#-->1.conditional flow control statement
'''
#2.loop flow control statement



#1.conditional flow control statement:

********************************

#*Controlling a flow of program execution based on condition is called a conditional flow control statement.

#*conditional statement is classified into 4 types,

#1.simple if
#2.if-else
#3.elif
#4.nested if-else

#1.simple if:
#************
#*if cond become True "if block/TSB" will get executed else it will just go with flow of program execution. 

#syntax:
#*******
#if cond:
#   stmt1 } 
#   stmt2 }---> TSB/if block
#   stmt3 }    

  :

"""
drawback of simple-if:
**********************
*if cond is False we don't have any specific block of code to execute, to over this we go "if-else"



2. if-else:
***********
*if cond become "True" "if block/TSB" will get executed else if cond become "False" "else block/FSB" will execute.
 
syntax:
*******
if cond:
   stmt1 } 
   stmt2 }---> TSB/if block
   stmt3 }   
else: 
    stmt5 }---> FSB/else block
    stmt6 }
"""
